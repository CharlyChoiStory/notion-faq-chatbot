# RAG 핵심 라이브러리
chromadb>=0.5.3
openai>=1.35.0
httpx>=0.27.2

# UI — streamlit 1.40+ / pillow 11.x have Python 3.14 pre-built wheels
streamlit>=1.40.0
pillow>=11.0.0

# 로컬 문서 업로드/파싱
pypdf>=4.3.1
python-docx>=1.1.2

# 환경 변수
python-dotenv>=1.0.1

# 유틸리티 — pre-built wheels only (no source build)
tqdm>=4.66.0
pandas>=2.2.0

# 선택 사항: OPENAI_API_KEY가 없을 때 로컬 임베딩 fallback용
sentence-transformers>=3.0.1

# cache-bust: 2026-05-18
